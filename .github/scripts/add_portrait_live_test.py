# The final portrait regression is now produced by finalize_portrait_pair_restore.py.
# Keep this legacy staging hook as a no-op so the retained validation workflow can
# be re-run without re-applying the older native-only test assertions.
print("final portrait live regression already staged")
